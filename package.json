{
  "name": "MustacheTemplate",
  "version": "0.0.0-dev",
  "description": "Kapitan: component to use the Mustache template processor on kubernetes resource configurations.",
  "main": "src/index.js",
  "keywords": [
    "kubernetes",
    "razee",
    "razeedash"
  ],
  "scripts": {
    "start": "node src/index.js",
    "test": "nyc --reporter=html --all --reporter=text mocha ",
    "test:debug": "mocha --inspect-brk",
    "check-coverage": "nyc check-coverage --statements 0 --branches 0 --functions 0 --lines 0",
    "lint": "npx npm-run-all eslint yamllint markdownlint",
    "eslint": "npx eslint src/",
    "yamllint": "npx yamllint .travis.yml kubernetes/MustacheTemplate/resource.yaml",
    "markdownlint": "npx markdownlint README.md docs/",
    "dockerlint": "npx dockerlint Dockerfile"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:razee-io/MustacheTemplate.git"
  },
  "contributors": [
    {
      "name": "Alex Lewitt"
    },
    {
      "name": "Adam King"
    }
  ],
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@razee/kapitan-core": "^0.4.0",
    "@razee/kubernetes-util": "0.0.6",
    "bunyan": "^1.8.12",
    "mustache": "^3.0.1",
    "object-path": "^0.11.4"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "dockerlint": "^0.3.9",
    "eslint": "^6.1.0",
    "markdownlint-cli": "^0.17.0",
    "mocha": "^6.1.4",
    "nock": "^10.0.6",
    "npm-run-all": "^4.1.5",
    "nyc": "^14.1.1",
    "rewire": "^4.0.1",
    "sinon": "^7.3.2",
    "yaml-lint": "^1.2.4"
  }
}
